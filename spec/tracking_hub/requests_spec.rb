require 'spec_helper'

describe TrackerHub::Request do
  it 'has a version number' do
    expect(TrackerHub::Request::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
